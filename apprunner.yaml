version: 1.0
runtime: python3
build:
  commands:
#    pre-build:
#      - wget -c https://s3.amazonaws.com/DOC-EXAMPLE-BUCKET/test-lib.tar.gz -O - | tar -xz
    build:
      - cd docassemble
        docker build -t 446815310803.dkr.ecr.us-west-2.amazonaws.com/cgc-app:latest .
#    post-build:
#      - python manage.py test
#  env:
#    - name: DJANGO_SETTINGS_MODULE
#      value: "django_apprunner.settings"
#    - name: MY_VAR_EXAMPLE
#      value: "example"
run:
#  runtime-version: 3.7.7
  command: docker run -d -p 80:80 -p 443:443 --stop-timeout 600 446815310803.dkr.ecr.us-west-2.amazonaws.com/cgc-app:latest
#  network:
    port: 80
#    env: MY_APP_PORT
#  env:
#    - name: MY_VAR_EXAMPLE
#      value: "example"